import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  searchTouchable: {
    position: 'absolute',
    top: -40,
    right: 20,
  },
  image: {
    resizeMode: 'cover',
    width: 30,
    height: 30,
  },
})
